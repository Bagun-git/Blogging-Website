<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Dashboard | MYBLOGSPACE</title>
  <link rel="stylesheet" href="/dashboard.css">
</head>
<body>
  <div class="container">
    <h1>Welcome to your Dashboard, <%= user.fullname %>!</h1>
    
    <h3>Your Statistics:</h3>
    <ul>
      <li>Total Blogs: <%= blogs.length %></li>
      <li>Followers: <%= user.followers.length %></li>
      <li>Following: <%= user.following.length %></li>
      <li>Total Likes: <%= blogs.reduce((sum, b) => sum + (b.likes ? b.likes.length : 0), 0) %></li>
      <li>Total Dislikes: <%= blogs.reduce((sum, b) => sum + (b.dislikes ? b.dislikes.length : 0), 0) %></li>
    </ul>

    <h3>Recent Blogs:</h3>
    <ul>
      <% blogs.forEach(blog => { %>
        <li>
          <strong><%= blog.title %></strong> — <%= blog.createdAt.toDateString() %>
          <a href="/blog/<%= blog._id %>" class="show-btn">Show Blog</a>
        </li>
      <% }) %>
    </ul>

    <a href="/profil" class="back-btn"><button>Back to Profile</button></a>
  </div>
</body>
</html>
