<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Profile | MYBLOGSPACE</title>
  <link rel="stylesheet" href="/edit-profile.css" />
</head>
<body>
  <div class="sidebar">
    <h2>MYBLOGSPACE</h2>
    <nav>
      <a href="/home">Home</a>
      <a href="/notification">Notifications</a>
      <a href="/messages">Messages</a>
      <a href="/profil" class="active">Profile</a>
      
    </nav>
  </div>

  <div class="main-content">
    <h2>Edit Profile</h2>
    <form action="/update-profile" method="POST" enctype="multipart/form-data" class="edit-form">
      <div class="form-group">
        <label for="fullname">Full Name</label>
        <input type="text" id="fullname" name="fullname" value="<%= user.fullname %>" required>
      </div>

      
      <div class="form-group">
        <label for="bio">Bio</label>
        <textarea id="bio" name="bio" rows="3"><%= user.bio || '' %></textarea>
      </div>

      <div class="form-group">
        <label for="profilePic">Profile Picture</label>
        <input type="file" id="profilePic" name="profilePic">
        <% if (user.profilePicPath) { %>
          <img src="<%= user.profilePicPath || 'profile-pic.jpg' %>" alt="Profile Picture" class="profile-pic" width="100" style="border-radius: 50%; margin-top: 10px;">
        <% } %>
      </div>

      <button type="submit" class="save-btn">Save Changes</button>
    </form>
  </div>
</body>
</html>
